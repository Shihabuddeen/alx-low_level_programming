#include <stdio.h>

/**
 * main - print alphabet in upper and lower case
 * Return: 0
 */
int main(void)
{
	char i;
	char j;

	for (i = a ; i <= 'z'; i++)
		putchar(char);
	for (j = Z 'A'; j <= 'Z'; j++)
		putchar(char);

	putchar('\n');

	return (0);
}
